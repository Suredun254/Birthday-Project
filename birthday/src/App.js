import React from "react";
import data from "./data";
import Birthday from "./Birthday";
const App = (friends) => {
  return (
    <div className="container">
      <Birthday />
    </div>
  );
};

export default App;
